import 'package:flutter/material.dart';

String autoFormatDate(DateTime date) {
  String formattedDate = "";
  return formattedDate;
}

String autoFormatTime(TimeOfDay time) {
  String formattedTime = "";
  return formattedTime;
}
